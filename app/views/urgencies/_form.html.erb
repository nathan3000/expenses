<%= form_for(@urgency) do |f| %>
  <% if @urgency.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@urgency.errors.count, "error") %> prohibited this urgency from being saved:</h2>

      <ul>
      <% @urgency.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
